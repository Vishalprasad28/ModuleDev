rsvplist.settings:
  type: config_object
  label: RSVP List Content Type Settings
  mapping:
    allowed_types:
      type: 'sequence'
      label: 'Content Types the RSVP Form Can Display On.'
      sequence:
        type: 'string'
        label: 'Content type'
